---  

--- Before Day-2 ---  
I already knew the basic Linux commands such as `pwd`, `ls`, and `cd`. I was familiar with running Python programs from the terminal and had a general understanding of files and directories. I had also used environment variables before, but I was not fully clear about how they worked internally.  
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------  

## Day-2 Checklist  

* [x] I understand what `PATH` is and why commands like `python` work without full paths
* [x] I can navigate the filesystem without clicking — using `cd`, `ls`, and `pwd` only
* [x] I can read, search, and inspect files using `cat`, `head`, `tail`, `grep`, and `wc`
* [x] I can edit a file using `nano` (open, edit, save, exit)
* [x] I understand pipes (`|`) and redirection (`>`, `>>`, `2>`) and can chain commands
* [x] I can set an environment variable in `.bashrc` and apply it with `source ~/.bashrc`
* [x] I know the difference between `export VAR=value` (available to child processes) and just `VAR=value` (shell-local)

--- After Day-2 ---  
I learned how the shell searches for executables using the `PATH` variable and why commands can be run without specifying their complete location. I became more comfortable navigating the filesystem entirely from the terminal. I learned how to inspect files efficiently using commands like `cat`, `head`, `tail`, `grep`, and `wc`. I also practiced editing files using `nano`, including saving changes and exiting the editor. Additionally, I understood how pipes and redirection allow commands to be combined and how environment variables can be configured permanently through `.bashrc`. The distinction between shell-local variables and exported environment variables is now much clearer.  
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------  

--- Feedback (Suggestions for the TDS Team) ---  
The hands-on exercises were helpful for understanding terminal usage. More practice questions involving pipes, redirection, and environment variables would make the concepts even easier to remember. A few real-world examples demonstrating how these commands are used in software development workflows would also be beneficial.  
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------  

---  
